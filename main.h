#ifndef _MAIN_H_
#define _MAIN_H_

#include <stdarg.h>
#include <stdlib.h>
#include <unistd.h>
int _printf(const char *format, ...);
int x_putchar(char ch);
int x_putint(int y);
int x_putstr(char *str);

#endif
